#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../elm/bin/elm-reactor" "$@"
  ret=$?
else 
  node  "$basedir/../elm/bin/elm-reactor" "$@"
  ret=$?
fi
exit $ret
